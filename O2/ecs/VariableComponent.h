#ifndef ECS_VARIABLE_COMPONENT_H
#define ECS_VARIABLE_COMPONENT_H

namespace ecs {
/// A flag for signaling that a component has an N->M mapping instead of N->N
class IVariableComponent {};
}

#endif
